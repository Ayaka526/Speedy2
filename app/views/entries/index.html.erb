
<div class="container">
  <% @entries.each do |entry| %>
    <div class="panel panel-default">
      <div class="panel-body">
         <span class="record-history" href="<%= entry.url%>"><%= entry.title %><span> - <i> published <%= entry.published.strftime("%Y-%m-%d-%R") %></i>
      </div>
    </div>
  <% end %>
</div>
